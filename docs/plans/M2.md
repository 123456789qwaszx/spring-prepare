# M2. 회차·세이브 업로드/복구 — 계획서

> PLAN.md §4 M2 기준. 상태: **대기**. 마지막 갱신: 2026-08-28 (초안)

---

## 1. 목표와 한 줄 요약

**클라 로컬 세이브를 서버에 올리고 새 기기에서 내려받는다. 외래키가 잘못된 세이브를 500이 아니라 404로 막는다.**

끝나면: 회차(playthrough)를 만들고, 슬롯에 PUT할 때마다 `revision`이 1, 2, 3… 오르며, 기기(`devices`)가 자동 등록되고, GET으로 스냅샷을 그대로 돌려받는다.

## 2. 선행 조건

- [ ] M1 `검증됨` (세이브는 특정 **버전**의 챕터를 가리키므로 `chapter_contents` 행이 있어야 한다).
- [ ] **PLAN#2 결정**: 슬롯 개수 제한 (권장 3).
- [ ] M1에서 `ChapterContentRepository.findId(chapterId, version)` 준비됨.

## 3. 숙련자의 사고흐름

1. **"upsert"를 DB 문장 하나로 만든다.** `SELECT 후 INSERT or UPDATE`는 두 요청이 겹치면 둘 다 INSERT를 시도해 UNIQUE 위반이 난다. `INSERT … ON DUPLICATE KEY UPDATE`는 `(playthrough_id, slot_no)` UNIQUE를 이용해 원자적으로 처리한다. **UNIQUE 제약이 곧 upsert의 키**라는 것이 M2의 학습.
2. **revision 규칙을 SQL에 정확히 쓴다.** 신규 INSERT 시에는 `ON DUPLICATE KEY UPDATE` 절이 실행되지 않으므로 INSERT 값에 `revision = 1`, UPDATE 절에 `revision = revision + 1`. 응답의 revision은 upsert 후 **다시 SELECT**해서 돌려준다 (M4에서 이 값이 충돌 감지 키가 된다).
3. **FK 위반을 미리 잡는다.** `chapterId + chapterVersion`이 없으면 DB는 FK 위반(500 계열)을 내지만, 클라에게는 "그 콘텐츠 버전이 서버에 없다"(404)가 맞는 답이다. 서비스에서 먼저 조회한다. 단, 조회와 INSERT 사이에 콘텐츠가 삭제될 경쟁은 무시한다 (콘텐츠는 삭제 API가 없다).
4. **기기 등록은 부수 효과.** `deviceKey`로 `devices` upsert (`user_id, device_key` UNIQUE), `last_seen_at` 갱신, id 확보. `INSERT … ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id), last_seen_at = NOW()` 트릭으로 한 문장에 id를 얻는다 — 트릭이므로 주석 필수.
5. **스냅샷은 열지 않는다.** 요청 DTO에서 `snapshot`은 `JsonNode`로 받되 어떤 키도 읽지 않고 `writeValueAsString`으로 문자열화해 JSON 컬럼에 넣는다. (PLAN M2 함정. `String`으로 받으면 요청 JSON 안에 JSON 문자열을 이스케이프해 넣어야 해 클라가 불편하다.)
6. **userId는 아직 경로에 노출된다.** M6 인증 전까지 `/users/{userId}/playthroughs`는 누구나 호출 가능. 알고 넘어간다.
7. **슬롯 제한은 앱에서.** `slot_no`가 `TINYINT`라 DB는 127까지 받는다. "1..3"은 비즈니스 규칙이므로 서비스에서 400. (DB `CHECK` 제약도 가능하지만 이 규칙은 바뀔 수 있어 앱에 둔다 — 이 판단 자체를 주석으로.)

## 4. 작업 목록과 순서

| # | 작업 | 산출물 | 상태 | 비고 |
|---|---|---|---|---|
| M2-0 | PLAN#2 결정 → D-007 | docs | 대기 | 권장 3 |
| M2-1 | `playthrough/`: record, Repository(INSERT, 목록 + slotCount 서브쿼리, end), Service, Controller | `playthrough/*` | 대기 | `ended_at` NULL → 진행 중 |
| M2-2 | `save/DeviceRepository.upsert(userId, deviceKey)` → id | `save/DeviceRepository.java` | 대기 | `LAST_INSERT_ID(id)` 트릭 |
| M2-3 | `save/SaveSlotRepository`: upsert, 슬롯 목록(스냅샷 제외), 슬롯 단건(스냅샷 포함) | `save/*` | 대기 | 목록 쿼리는 `snapshot` 컬럼을 SELECT하지 않는다 (큰 컬럼) |
| M2-4 | `save/SaveSlotService.upsert(...)` `@Transactional`: 콘텐츠 id 조회(404) → 기기 upsert → 슬롯 upsert → 재조회 | `save/SaveSlotService.java` | 대기 | 회차 소유자 검증은 M6 |
| M2-5 | `save/SaveSlotController`: PUT/GET 2개 + 요청 DTO(`snapshot: JsonNode`) | | 대기 | |
| M2-6 | 통합 테스트: revision 1→2, 없는 버전 404, 스냅샷 왕복 동일, 기기 2행, 슬롯 4 → 400 | test | 대기 | `DbCleaner` 확장 (save_slots, devices, playthroughs) |
| M2-7 | `docs/M2-check.md` | docs | 대기 | |
| M2-8 | 실행 검증 → 문서 갱신 → 커밋 `M2: 세이브 업로드` → **계획서 전체 점검** | | 대기 | |

## 5. 특이사항

- **`INSERT … ON DUPLICATE KEY UPDATE`의 반환값**: 신규 1, 갱신 2, 값이 같아 변경 없으면 0. `revision+1`이 항상 바뀌므로 0은 나오지 않지만, 이 규칙을 모르면 "왜 2가 나오지?"가 된다.
- **`updated_at … ON UPDATE CURRENT_TIMESTAMP`**: 행 값이 실제로 바뀔 때만 갱신. revision이 항상 바뀌므로 문제 없음.
- **JSON 컬럼 왕복**: M1의 PLAN#1 결정이 그대로 적용된다. 스냅샷은 정규화되어 돌아온다 → "의미상 동일"로 검증.
- **목록 API가 스냅샷을 빼는 이유**: 스냅샷은 수십 KB가 될 수 있고 목록은 UI용이다. 쿼리 단계에서 컬럼을 빼는 것과 DTO에서 빼는 것은 네트워크·메모리 비용이 다르다 — SELECT 컬럼 목록을 명시하는 습관.
- **학습 포인트**: UNIQUE = upsert 키. NULL 허용 FK(`device_id`).

## 6. 주의사항

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| C1 | 첫 PUT의 revision이 0 | INSERT 값에 revision을 안 넣어 DEFAULT 0 | INSERT에 1 명시 |
| C2 | 두 번째 PUT도 revision 1 | UPDATE 절에 `revision = revision + 1` 누락, 또는 `VALUES(revision)` 사용 | UPDATE 절 확인 |
| C3 | 기기 upsert 후 id가 0 또는 엉뚱한 값 | `LAST_INSERT_ID(id)` 트릭 없이 KeyHolder 사용 → 갱신 경로에서 키 없음 | 트릭 사용 또는 SELECT로 재조회 |
| C4 | 없는 chapterVersion PUT이 500 | 서비스 사전 조회 누락 | `findId` → `NotFoundException` |
| C5 | 스냅샷 안의 문자열이 `\"`로 이스케이프된 채 저장 | `snapshot`을 `String`으로 받아 다시 JSON 문자열로 감쌈 | `JsonNode`로 받아 `writeValueAsString` |
| C6 | `slot_no` 200 PUT이 500 | TINYINT 범위 초과(`Out of range`) | 서비스에서 1..N 검증 → 400 (C8과 같은 층) |
| C7 | 목록의 `device`가 null | `device_id` NULL 허용, LEFT JOIN 필요 | `LEFT JOIN devices` — INNER JOIN이면 행이 사라진다 |
| C8 | 남의 회차에 PUT이 된다 | 인증·소유 검증은 M6 | 지금은 의도된 상태 |
| C9 | `POST /playthroughs/{id}/end` 두 번 → 두 번째도 200 | 멱등하게 둘지, 409로 할지 | 멱등(200) 권장. `ended_at`이 이미 있으면 갱신하지 않음 |

## 7. 완료 기준 체크리스트

- [ ] PUT → revision 1. 같은 슬롯 PUT → revision 2.
- [ ] 없는 `chapterVersion` PUT → 404 (500 아님).
- [ ] GET의 `snapshot`이 올린 것과 의미상 동일.
- [ ] `deviceKey` 두 종류 → `devices`에 두 행, `last_seen_at` 갱신.
- [ ] 슬롯 번호 범위 밖 → 400.

## 8. 이 M에서 갱신할 문서

| 작업 끝 | 갱신 |
|---|---|
| M2-0 | DECISIONS.md D-007 |
| M2-1~M2-7 | 이 표 §4, STATUS.md |
| M2-8 | STATUS.md, **M3~M9 계획서 점검** (M3의 PUT 확장 형식, M4의 revision 활용) |

## 9. 열린 결정 / 다음 M에 넘기는 것

- PLAN#2 → M2-0.
- M3에 넘김: PUT 요청 DTO에 `choices`, `events` 배열이 추가된다 — M2 DTO를 **확장 가능한 모양**(record + 선택 필드 null 허용)으로 둔다.
- M4에 넘김: `revision` 재조회 로직이 낙관적 동시성의 기반. M2의 upsert SQL은 M4에서 `WHERE revision = :base` 조건부 UPDATE로 갈라진다.
