# M6. 마감 — 계획서

> PLAN.md §4 M6 기준. 상태: **착수 대기**. 마지막 갱신: 2026-08-30 (D-011·D-012 반영)

---

## 1. 목표와 한 줄 요약

**Unity 클라가 붙어도 되는 상태.** 비밀번호 해시, 토큰 인증·소유 검증, 관리자 키, 에러 형식 통일, 스키마 결정 마감, README.

끝나면: 토큰 없이 세이브 API → 401, 남의 회차 → 403, DB에 평문 비밀번호 없음, 모든 에러가 같은 형식.

## 2. 선행 조건

- [x] M5 `검증됨` (2026-08-30).
- [x] **PLAN#4 결정** → **D-011**: `(playthrough_id, event_key)` 로 좁힌다. 컬럼은 남기고 UNIQUE 에서만 뺀다.
  - **파생 효과에 주의**: 범위가 넓어져 **409 가 더 자주 난다.** 버전을 올린 뒤의 재도달이 이제 막히고,
    지금 `EventLogRepository.insertAll` 은 중복이면 요청 **전체**가 409 다. M4 가 choices 를 `replayed` 로 흡수했듯
    이벤트도 "이미 있으면 빼고 넣기" 가 필요하다 (M6-2b).
- [x] **PLAN#5 결정** → **D-012**: 도입한다. 그리고 **`game` 을 다시 만든다** (baseline 이 아니라).
  - M0에서 확보한 근거: `game`과 `game_test`에 `schema.sql`을 **손으로 두 번** 적용했다(두 줄을 고쳐 실행 → 되돌리기). M이 진행되며 마이그레이션이 쌓이면 이 수작업이 매번 반복되고, 한쪽만 적용하는 순간 "테스트만 통과/실패"가 난다(ANALYSIS R4).
  - **M1에서 실제로 터졌다** (ANALYSIS §4.1): `game`에 테이블이 `users` 하나뿐이었고, M0는 `users`만 써서 완료 기준을 전부 통과하고도 드러나지 않았다. M1의 `ALTER TABLE`에서야 `Error 1146`으로 나타났다. 게다가 `schema.sql`은 `CREATE TABLE`에 `IF NOT EXISTS`가 없어 **재실행이 불가능**해서, 복구에 별도의 손 SQL이 필요했다(M1 §7-1 F14).
  - **M5에서 한 번 더 쌓였다**: `V3__stats_indexes.sql`을 `game`과 `game_test`에 **손으로 두 번** 적용했다. 그리고 이 파일은 재실행이 불가능하다 — 두 번 돌리면 `1061 Duplicate key name` / `1091 Can't DROP`이다. **"어디까지 적용했나"를 사람이 기억해야 하는 상태**가 M0→M5 내내 이어졌고, 이제 마이그레이션이 V2·V3 둘이라 기억할 것도 늘었다.
  - 따라서 Flyway 도입 시 두 가지를 함께 해결한다: (a) `game`·`game_test` 양쪽에 같은 마이그레이션 자동 적용, (b) `flyway_schema_history`로 **적용 이력이 남아** 드리프트가 조용히 생기지 못하게. `V1__init.sql`은 `schema.sql`을 그대로 옮기되 이 기회에 재실행 가능성도 검토한다.
  - **인코딩을 반드시 지정한다** (M5 §7-1 F35): `spring.flyway.encoding=UTF-8`. 기본값에 맡기면 한국어 Windows에서 MS949로 읽어 한글이 깨진 채 들어가고, **SQL 문법도 행 수도 멀쩡해서 조용히 통과한다.**
- [ ] M0~M5에서 미뤄둔 것 목록 (아래 §9의 "받은 것") 확인.
- [ ] **세션 변수를 건드리는 스크립트에 주의** (M5 §7-1 F34). `SET` 으로 바꾼 세션 변수는 트랜잭션이 아니라 **커넥션**에 붙어 있고, 풀은 그것을 되돌려 주지 않는다. M5에서 seed의 `SET SQL_SAFE_UPDATES = 1`이 무관한 테스트 6건을 죽였다. **"원상복구"는 원래 값을 알 때만 옳다.**

## 3. 숙련자의 사고흐름

1. **Security 전체가 아니라 `spring-security-crypto`만.** 필요한 것은 `BCryptPasswordEncoder` 하나. 전체 Security를 넣으면 필터 체인·CSRF·기본 로그인 페이지가 따라와 학습 범위를 벗어난다. "필요한 최소 의존성" 원칙.
2. **인터셉터 하나로 인증·인가를 다 한다.** `HandlerInterceptor`가 `Authorization: Bearer <token>`을 `sessions`에서 조회 → 만료 확인 → 요청 속성에 `userId`. 경로 패턴은 `/playthroughs/**`, `/users/{id}/**`. 소유 검증(403)은 인터셉터가 아니라 **서비스**에서 (회차의 user_id를 알아야 하므로). 단, `/users/{id}`의 id ≠ 토큰의 userId는 인터셉터에서 바로 403.
3. **토큰은 랜덤 문자열 + DB 행.** JWT는 M6 범위 밖 (PLAN "하지 않는 것"). `sessions(token CHAR(64) PK, user_id, expires_at)`. 토큰은 `SecureRandom` 32바이트 hex. 로그아웃은 행 삭제.
4. **관리자 키는 프로퍼티.** `POST /content/**`는 `X-Admin-Key` 헤더 == `app.admin-key`. 값은 `application-local.properties`. 커밋되는 파일에 기본값을 두지 않는다.
5. **에러 형식은 "누락 케이스 채우기".** M0의 `ErrorResponse`가 이미 있으므로, `@ExceptionHandler(Exception.class)` → 500 통일, `HttpMessageNotReadableException`(JSON 파싱 실패) → 400, `MethodArgumentTypeMismatchException` → 400, 401/403 전용 예외. M4의 409 요약은 `detail`에.
   - **우선순위가 있다.** M2 검증에서 `HttpMessageNotReadableException` 이 실제로 새는 것을 확인했다 — 깨진 JSON 을 보내면 Spring 기본 형식이 나가서 클라가 `code` 로 분기할 수 없다. 이건 "혹시 모를 케이스"가 아니라 **평범한 클라 버그에서 매번 나오는 응답**이므로 M6-8 의 첫 항목으로 잡는다.
   - **응답 `Content-Type` 도 엔드포인트마다 다르다** (M3 검증 §7). `ResponseEntity<String>` 을 쓰는 M1의 콘텐츠 배포는 `StringHttpMessageConverter` 가 처리해 `charset=UTF-8` 이 붙고, 객체를 돌려주는 나머지는 Jackson 컨버터가 처리해 charset 이 없다. **무해하다** — JSON은 규격상 UTF-8이라 charset 을 안 붙이는 쪽이 오히려 정석이고, 실제로 깨진 것은 서버가 아니라 PowerShell 의 디코딩이었다. 다만 "형식 통일"을 하는 M이니 **의도한 차이인지 우연인지 한 번은 결정하고 넘어간다.** (권장: 그대로 둔다. `ResponseEntity<String>` 쪽을 `ResponseEntity<byte[]>` 로 바꾸면 charset 이 사라져 통일되지만, 그 대가로 원본 바이트 배포의 의도가 흐려진다.)
6. **스키마 변경은 마이그레이션으로.** D-011은 `V4__event_once_per_playthrough.sql` (기존 UNIQUE 드롭 → 새 UNIQUE). 기존 데이터가 새 UNIQUE를 위반하면 마이그레이션이 실패한다 — 그래서 M6-1b의 재생성이 M6-2보다 앞에 온다.
7. **baseline 이 아니라 재생성** (D-012). baseline 은 "V1~V3 는 이미 적용돼 있다고 **믿어라**" 라고 Flyway에게 말하는 것이다. 그 믿음이 틀렸을 때가 정확히 M1의 R4였고, 그때는 `Error 1146`이 날 때까지 아무도 몰랐다. **`seed.sql`이 있으니 재생성 비용이 거의 0**이고, 재생성은 마이그레이션이 **실제로 도는지**를 한 번 증명해 준다 — 이것은 baseline 이 영영 해 주지 못하는 일이다. 따라서 `baseline-on-migrate` 는 쓰지 않는다.
8. **README는 마지막에.** 실행법·API 목록·마일스톤 상태. `docs/`의 내용을 요약하지 말고 링크한다.

## 4. 작업 목록과 순서

| # | 작업 | 산출물 | 상태 | 비고 |
|---|---|---|---|---|
| M6-0 | PLAN#4, PLAN#5 결정 → D-011, D-012 | docs | **완료** | 둘 다 권장안. #5 는 baseline 이 아니라 **재생성** |
| M6-1 | Flyway 도입: 의존성, 마이그레이션을 클래스패스로 이동, `V1__init.sql`(= schema.sql), `spring.flyway.encoding=UTF-8` | build, `src/main/resources/db/migration/` | 대기 | 의존성은 `flyway-core` + **`flyway-mysql`** (Flyway 9+ 부터 MySQL 은 별도 모듈). `spring-boot-starter-flyway` 라는 이름은 **없다** — 추측하지 않는다(PLAN §2.2) |
| M6-1b | `game`·`game_test` 재생성: drop → create → 기동(Flyway 가 V1~V5 적용) → `seed.sql` | | 대기 | **baseline 을 쓰지 않는 이유가 여기 있다** — 마이그레이션이 실제로 도는지를 이 단계가 검증한다 (D-012) |
| M6-2 | `V4__event_once_per_playthrough.sql` (D-011) | migrations | 대기 | 적용 전 중복 확인: `SELECT playthrough_id, event_key, COUNT(*) … HAVING COUNT(*) > 1` → 0행 |
| M6-2b | **이벤트 중복 흡수** — `EventLogRepository.existingEventKeys(playthroughId, keys)` + 서비스에서 걸러 넣기 | `save/*` | 대기 | D-011 의 파생 효과. 기존 `existingEpisodeIds` 는 `(playthrough, content, episode)` 기준이라 새 UNIQUE 와 안 맞는다. 응답의 `acceptedEvents` 가 여기서 실제 정보를 담기 시작한다 |
| M6-2c | `V5__sessions.sql` | migrations | 대기 | M5 가 V3 를 썼고 D-011 이 V4 를 쓰므로 **세션 테이블은 V5** 다 |
| M6-3 | 비밀번호 해시: `spring-security-crypto`, `UserService.create`에서 encode. 기존 평문 행은 삭제 (M0~M5 테스트 데이터) | | 대기 | `password VARCHAR(100)`은 BCrypt 60자에 충분 |
| M6-4 | `auth/`: `POST /auth/login` → 토큰, `POST /auth/logout`, `SessionRepository` | `auth/*` | 대기 | 만료 24h (프로퍼티) |
| M6-5 | `common/AuthInterceptor` + `WebMvcConfigurer` 등록, `UnauthorizedException`(401), `ForbiddenException`(403) | | 대기 | |
| M6-6 | 소유 검증: `PlaythroughService`·`SaveSlotService`에서 `playthrough.user_id == 요청 userId` | | 대기 | |
| M6-7 | `AdminKeyInterceptor` for `/content/**` POST | | 대기 | GET은 공개 (클라가 콘텐츠 내려받음) |
| M6-8 | 에러 형식 통일: 남은 예외 핸들러 추가, 모든 테스트의 에러 본문 단언 | | 대기 | **M2 검증에서 실제 구멍 확인**: 깨진 JSON 을 보내면 `HttpMessageNotReadableException` 이 `GlobalExceptionHandler` 를 거치지 않고 Spring 기본 형식(`{timestamp,status,error,path}`)으로 나간다. 이것부터 잡는다 |
| M6-9 | M0~M5에서 미룬 것 처리: `spring.profiles.active` 위치, `contextLoads` 프로필, Validation 스타터 여부, Location 절대 URI | | 대기 | 각각 D-0xx로 결정 |
| M6-10 | README.md 갱신, `docs/M6-check.md` | docs | 대기 | **`docs/schema.sql` 과 `V1__init.sql` 중 어느 쪽이 정본인지 적는다** — 같은 내용을 담은 파일이 둘이 된다 (D-012) |
| M6-11 | 실행 검증 → 커밋 `M6: 마감` → **계획서 전체 점검** | | 대기 | |

## 5. 특이사항

- **첫 의존성 추가 M.** `spring-security-crypto`, (Flyway). 그 외는 여전히 넣지 않는다.
- **기존 API 호환이 깨진다.** M0~M5의 curl 시나리오는 토큰 없이 동작했다. M6 이후 `M{n}-check.md`는 로그인 단계를 앞에 붙여야 한다 → 각 check 문서 머리에 "M6 이후: 토큰 필요" 주석.
- **Flyway 의존성 이름** — 초안에 `spring-boot-starter-flyway` 라고 적어 뒀던 것은 **틀렸다.** 그런 스타터는 없다. 올바른 좌표는 `org.flywaydb:flyway-core` + `org.flywaydb:flyway-mysql` 이고, 버전은 Boot 의 의존성 관리가 정해 준다. **이름을 지어내지 않는다**(PLAN §2.2) 는 원칙이 초안 자신에게 적용된 사례라 지우지 않고 남긴다.
- **학습 포인트**: 인증 vs 인가, 해시 vs 암호화, 인터셉터의 위치(DispatcherServlet 뒤, 컨트롤러 앞), 마이그레이션의 버전 관리(체크섬·이력 테이블·baseline 이 각각 무엇을 보장하고 무엇을 보장하지 않는가).

## 6. 주의사항

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| C1 | Flyway가 `game`에서 "Found non-empty schema without schema history table" | DB를 지우지 않고 기동했다 | **`baseline-on-migrate` 를 켜지 않는다** — 이 에러는 D-012 가 막으려는 상황을 정확히 알려주는 신호다. DROP → CREATE 후 다시 기동 |
| C2 | V4 UNIQUE 마이그레이션 실패 (1062) | 기존 데이터가 새 범위 위반 | 재생성 직후라면 날 수 없다. 나면 **순서가 틀린 것** — M6-1b 가 M6-2 보다 앞이다 |
| C2b | 한글이 `?꽦?떎` 로 들어감 | `spring.flyway.encoding` 미지정 → 플랫폼 기본(MS949) | `spring.flyway.encoding=UTF-8`. **SQL 은 멀쩡히 통과하므로 숫자 테스트로는 못 잡는다** (F35) |
| C3 | 로그인 후에도 401 | 헤더 형식 `Bearer ` 접두사, 인터셉터 경로 패턴 | `addPathPatterns` 확인, `/auth/**`·`/users`(POST) 제외 |
| C4 | 본인 회차인데 403 | 소유 검증이 userId 타입 불일치(Long vs long, String) | 요청 속성 타입 통일 |
| C5 | 콘텐츠 GET이 401 | 관리자 인터셉터가 GET까지 잡음 | 메서드 조건 `POST`만 |
| C6 | 기존 테스트 전부 401 | 테스트에 토큰 없음 | 테스트 지원 `AuthSupport.loginAs(...)` 헬퍼 |
| C7 | BCrypt가 느려 테스트가 느림 | 기본 strength 10 | 테스트 프로필에서 strength 4 (프로퍼티) |

## 7. 완료 기준 체크리스트

- [ ] 토큰 없이 세이브 API → 401. 남의 회차 → 403.
- [ ] `SELECT password FROM users`에 평문 없음 (`$2a$…`).
- [ ] 모든 4xx/5xx가 `{code, message, detail?}`.
- [ ] `X-Admin-Key` 없이 콘텐츠 POST → 401.
- [ ] README에 실행법·API·상태.

## 8. 이 M에서 갱신할 문서

| 작업 끝 | 갱신 |
|---|---|
| M6-0, M6-9 | DECISIONS.md D-011~ |
| M6-1 | ANALYSIS §1.4 제약·R4 갱신, README(루트) |
| M6-8 | `M0~M5-check.md` 머리에 토큰 주석 |
| M6-11 | STATUS.md, **M7~M9 계획서 점검** |

## 9. 열린 결정 / 받은 것 / 넘기는 것

- 받은 것: M0 (`spring.profiles.active` 위치, `contextLoads` 프로필, Validation, Location URI), M3 (`event_log` UNIQUE), M4 (409 형식).
- 넘기는 것: M7 — 로그인 API 형식, 토큰 저장 위치(Unity `PlayerPrefs` vs 파일).
