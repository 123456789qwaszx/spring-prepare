# M4. 멱등성과 충돌 — 계획서

> PLAN.md §4 M4 기준. 상태: **대기**. 마지막 갱신: 2026-08-28 (초안)
> **이 프로젝트의 핵심 학습 M.** 코드 양은 적고 사고가 많다.

---

## 1. 목표와 한 줄 요약

**네트워크 재시도와 두 기기 동시 플레이를 락 없이 데이터(revision, UNIQUE)로 처리한다.**

끝나면: 같은 요청을 두 번 보내도 두 번째는 아무것도 쓰지 않고 `replayed=true`, 두 기기가 어긋난 revision으로 올리면 하나만 성공하고 다른 쪽은 409 + 서버 요약, force 재요청은 새 것만 추가한다. 두 스레드 동시 PUT 테스트가 "정확히 하나만 200"을 증명한다.

## 2. 선행 조건

- [ ] M3 `검증됨` (같은 seq 재전송이 409로 끝나는 것을 확인했음).
- [ ] **PLAN#3 결정**: 요청 UUID 멱등 키 도입 여부 (권장: choices 기반으로 시작).
- [ ] MySQL 격리 수준 확인: `SELECT @@transaction_isolation;` → `REPEATABLE-READ` (기본). 바꾸지 않는다.

## 3. 숙련자의 사고흐름

1. **문제를 두 개로 쪼갠다.** (a) *재전송*: 같은 클라가 같은 요청을 다시 보냄 — 응답을 못 받았을 뿐 서버는 이미 적용했다. (b) *충돌*: 다른 기기가 사이에 끼어들었다. 둘 다 "baseRevision ≠ 서버 revision"으로 보이지만 답이 다르다 — (a)는 200, (b)는 409.
2. **재전송을 어떻게 알아보나.** `서버 revision == baseRevision + 1` 이고 동봉 `choices`의 `(save_slot_id, seq)`가 **전부 이미 존재**하면 "내가 방금 적용한 그 요청"이다. choices가 비어 있으면 판정 불가 → 200 + 현재 상태 (PLAN 함정). 이 구멍을 요청 UUID로 막을지가 PLAN#3.
3. **충돌 감지는 UPDATE 한 줄.**
   ```sql
   UPDATE save_slots SET ..., revision = revision + 1
   WHERE id = :id AND revision = :baseRevision
   ```
   영향 행 수 0 = 누군가 먼저 바꿨다 = 409. 이것이 **낙관적 동시성**이다. `SELECT … FOR UPDATE`(비관적 락)를 쓰지 않는 이유: 충돌이 드물고, 락은 커넥션을 붙들며, 완료 기준의 동시 요청 테스트가 락 없이도 정확함을 증명한다.
4. **순서가 곧 정확성.** 서비스 안: (1) 현재 슬롯 SELECT (없으면 신규 경로) → (2) replayed 판정 → (3) 조건부 UPDATE, 0행이면 409 → (4) choices/events 배치. (3)이 (4)보다 먼저여야 충돌 시 이력이 안 쌓인다. 같은 트랜잭션이므로 (4) 실패 시 (3)도 롤백.
5. **동시성 테스트는 "정확히 하나"를 단언한다.** `ExecutorService`로 두 스레드가 같은 baseRevision으로 PUT → 200 하나, 409 하나. "둘 다 200"이면 조건부 UPDATE가 빠진 것, "둘 다 409"면 첫 SELECT를 트랜잭션 밖에서 하고 있는 것. 이 테스트를 M4 **첫 작업**으로 써 두고(실패하는 상태로) 구현이 끝나면 통과시킨다 — 테스트가 요구사항이다.
6. **force는 "새 것만 추가".** `?force=true&baseRevision=<서버 revision>`이면 조건부 UPDATE는 그대로 통과하고, choices/events의 UNIQUE 충돌만 무시한다. `INSERT IGNORE`는 **모든** 오류를 경고로 바꿔 FK 위반까지 삼키므로, 사전 조회로 이미 있는 seq를 걸러내는 쪽을 권장. (이유를 주석으로.)
7. **REPEATABLE READ에서 위 패턴이 안전한 이유**를 한 문단으로 문서에 남긴다: UPDATE는 항상 최신 커밋된 행에 대해 조건을 평가하고 행 락을 잡는다 (스냅샷이 아니라 "current read"). 그래서 두 트랜잭션 중 하나만 `revision = base`를 만족한다.

## 4. 작업 목록과 순서

| # | 작업 | 산출물 | 상태 | 비고 |
|---|---|---|---|---|
| M4-0 | PLAN#3 결정 → D-010 | docs | 대기 | |
| M4-1 | **실패하는 동시성 테스트 먼저**: `SaveSlotConcurrencyTest` (ExecutorService 2 스레드, CountDownLatch로 동시 출발) | test | 대기 | 지금 상태에선 둘 다 200 → 실패 |
| M4-2 | 요청에 `baseRevision` 추가, `force` 쿼리 파라미터 | DTO/Controller | 대기 | `baseRevision` 없으면 400 (M2 호환을 끊는다 — 의도) |
| M4-3 | `SaveSlotRepository.updateIfRevision(...)` (조건부 UPDATE, 영향 행 수 반환), `ChoiceHistoryRepository.existingSeqs(slotId, seqs)` | | 대기 | |
| M4-4 | `SaveSlotService.upsert` 재작성: 신규/replayed/정상/충돌/force 다섯 경로 | | 대기 | 응답 `replayed`, 409 본문에 서버 요약 |
| M4-5 | `common/ConflictException` → 409 + 요약 본문 (`detail`에 서버 상태) | | 대기 | `ErrorResponse.detail`이 객체가 되는 첫 사례 → `Object`로 넓힐지 D-004 갱신 |
| M4-6 | 통합 테스트: 재전송 replayed, A/B 충돌 409, force 후 상태, 동시성 통과 | test | 대기 | |
| M4-7 | `docs/M4-check.md` — 두 터미널로 재현하는 절차 | docs | 대기 | |
| M4-8 | 실행 검증 → 문서 갱신 → 커밋 `M4: 멱등성과 충돌` → **계획서 전체 점검** | | 대기 | |

## 5. 특이사항

- **락을 쓰지 않는 것이 "하지 않는 것"이다.** 완료 기준이 그 정당성을 증명한다. 비관적 락을 써 보고 싶다면 M9 비교 과제로.
- **신규 슬롯 + baseRevision**: 슬롯이 없을 때 baseRevision은 0이어야 한다. 0이 아니면 400("슬롯이 없는데 base가 있다").
- **`ErrorResponse.detail`의 타입**: M0에서 `String`으로 두었다면 M4의 409 요약(객체)을 담기 위해 `Object`로 넓히거나 `ConflictResponse`를 따로 둔다. 후자 권장 (에러 형식 통일은 M6에서 다시 본다).
- **학습 포인트**: 낙관적 동시성, 멱등성의 두 얼굴(재전송 vs 중복), `INSERT IGNORE`의 위험, 격리 수준과 current read.

## 6. 주의사항

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| C1 | 동시성 테스트에서 둘 다 200 | 조건부 UPDATE 없이 `ON DUPLICATE KEY UPDATE` 유지 | M2 upsert를 갈라서 기존 슬롯은 조건부 UPDATE |
| C2 | 둘 다 409 | 첫 SELECT가 트랜잭션 밖 / 테스트가 baseRevision을 잘못 줌 | 서비스 메서드 전체가 하나의 트랜잭션인지 |
| C3 | 테스트가 간헐적으로 실패 (flaky) | 두 스레드가 진짜 동시에 출발하지 않음 | `CountDownLatch`로 동시 출발, 풀 크기 ≥ 2 확인 (`spring.datasource.hikari.maximum-pool-size` 기본 10) |
| C4 | replayed인데 revision이 또 올랐다 | 판정 전에 UPDATE 실행 | 판정 → UPDATE 순서 |
| C5 | force 요청이 FK 위반을 삼켰다 | `INSERT IGNORE` | 사전 조회로 교체 |
| C6 | 409 응답이 `ErrorResponse` 형식이 아니다 | 별도 응답 타입 | 의도. M6 통일 시 `detail` 객체로 흡수 |
| C7 | Deadlock 로그 | 두 트랜잭션이 슬롯 행과 이력 행을 다른 순서로 잠금 | 항상 슬롯 UPDATE → 이력 INSERT 순서 (M4-4 순서). 재현되면 `SHOW ENGINE INNODB STATUS` |

## 7. 완료 기준 체크리스트

- [ ] 같은 요청 두 번 → 두 번째 200 + `replayed=true`, 테이블 불변.
- [ ] A(rev 3) 후 B(base 2) → B 409 + A의 요약(revision, updatedAt, device, currentEpisodeId, playSeconds).
- [ ] B force → 200, B 스냅샷, A 이력 유지 + B 새 이력 추가.
- [ ] 동시 PUT 테스트 → 정확히 하나 200, 하나 409. 10회 반복해도 동일.

## 8. 이 M에서 갱신할 문서

| 작업 끝 | 갱신 |
|---|---|
| M4-0 | DECISIONS.md D-010 |
| M4-5 | DECISIONS.md D-004 갱신(detail 타입) |
| M4-8 | STATUS.md, **M5~M9 계획서 점검** (M8 충돌 UI가 409 본문 형식에 의존) |

## 9. 다음 M에 넘기는 것

- M6: 에러 형식 통일 시 409 요약 포함.
- M8: 409 본문 필드 = 충돌 UI가 보여줄 것.
- M9: 비관적 락 버전과 비교 실험.
