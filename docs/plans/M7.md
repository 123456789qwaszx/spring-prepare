# M7. Unity — 저장 포트 구현 — 계획서

> PLAN.md §4 M7 기준. 상태: **대기**. 마지막 갱신: 2026-08-28 (초안)
> 작업 장소: **`ked-presentation-runtime`(Unity) 레포**. 이 서버 레포에는 "서버 쪽 확인 사항"만 반영된다.

---

## 1. 목표와 한 줄 요약

**`ked-presentation-runtime`이 비워둔 `ISaveStore`를 채운다. 로컬 우선, 서버는 동기화.**

끝나면: 비행기 모드에서 선택 5개 → 온라인 → 서버 `choice_history`에 5행(seq 연속). 앱 강제 종료 후 재실행 → 로컬에서 이어지고 큐가 남아 있다.

## 2. 선행 조건

- [ ] M6 `검증됨` (로그인·토큰이 있어야 Unity가 붙는다).
- [ ] Unity 레포 현황 재확인: `ProgressionDriver`(순수 Commit, 순서를 쥔 유일한 자리), `ProgressionLauncher`(호스트), `VNAppBootstrap`(조립), `RollbackHistory`/`ChoiceHistory`(Yarn 계층 [3]). **`ISaveStore`는 아직 없다** — 이 M에서 처음 정의.
- [ ] 서버 쪽 확인: M3 PUT 형식이 C#에서 만들기 쉬운가 (`chosenAt` ISO-8601 UTC → `DateTime.UtcNow.ToString("o")`).

## 3. 숙련자의 사고흐름

1. **드라이버는 건드리지 않는다.** `ProgressionDriver` 주석대로 "순서를 두 곳에 적으면 갈린다". 저장은 **호스트(`ProgressionLauncher`)가 커밋 이벤트를 받아** 한다. 드라이버가 커밋 사실을 밖에 알리는 최소 훅(이벤트 하나)만 추가하고, 무엇을 저장할지는 호스트가 정한다.
2. **로컬이 진실, 서버는 사본.** 오프라인 플레이가 요구사항이므로 저장은 항상 로컬 파일에 먼저, 서버 전송은 큐에서 비동기. 서버 실패는 게임 진행을 막지 않는다.
3. **큐에 쌓는 것은 "이벤트", 스냅샷은 "최신 하나".** 선택 커밋마다 `{seq, episodeId, optionIndex, chosenAt}`와 EventKey 발생을 큐에 적재. 스냅샷은 덮어쓰기. 그래서 M3 요청이 "증분 choices + 최신 snapshot" 모양이다 — 서버 API가 클라 큐 모양을 결정한 것이 아니라 **그 반대**임을 확인.
4. **seq는 클라가 매긴다.** 슬롯별 단조 증가, 로컬에 영속. 서버 UNIQUE `(save_slot_id, seq)`가 중복을 막는다 (M4). 앱이 죽어도 seq가 되돌아가면 안 되므로 seq 증가와 큐 적재를 한 파일 쓰기로 한다.
5. **스냅샷의 내용은 서버가 모른다.** `{ nodeName, lineId, Yarn 변수, StageState, ProgressionState }` 통째로. 서버는 열지 않는다 (PLAN 1.4). 스냅샷 직렬화 형식은 Unity 쪽 결정 — JSON이면 MySQL JSON 컬럼이 받고, 아니면 base64 문자열로 감싼다 (그러면 PLAN#1을 다시 봐야 한다).
6. **네트워크 계층은 얇게.** `UnityWebRequest` + 토큰 헤더. 재시도는 큐가 남아 있으면 다음 기회에 — 지수 백오프 정도만.

## 4. 작업 목록과 순서 (Unity 레포)

| # | 작업 | 산출물 | 상태 | 비고 |
|---|---|---|---|---|
| M7-1 | `ISaveStore` 인터페이스 확정: `Save(slot, snapshot)`, `Load(slot)`, `List()` + 결과 타입 | `Assets/Scripts/Save/ISaveStore.cs` | 대기 | 동기/비동기 결정 (권장 async Task) |
| M7-2 | `LocalFileSaveStore`: `Application.persistentDataPath/saves/slot{n}.json` | | 대기 | 원자적 쓰기(임시 파일 → rename) |
| M7-3 | `SyncQueue`: 선택·이벤트 적재, seq 영속, 스냅샷 최신 유지 | | 대기 | 파일 하나 `sync_queue.json` |
| M7-4 | 드라이버에 커밋 훅(이벤트) 추가 — 최소 변경 | `ProgressionDriver` | 대기 | 인자: chosen option, 새 state, episode의 EventKey |
| M7-5 | `ProgressionLauncher`에서 훅 구독 → `LocalFileSaveStore.Save` + `SyncQueue.Enqueue` | | 대기 | |
| M7-6 | `ServerSyncSaveStore`: 큐 → M3/M4 형식 PUT, 성공 시 큐 비우고 revision 저장, 409면 M8로 위임 | | 대기 | `baseRevision` 로컬 저장 |
| M7-7 | 서버 레포: `docs/M7-check.md`에 비행기 모드 시나리오, 필요 시 API 형식 조정 → D-0xx | docs | 대기 | |
| M7-8 | 검증 → 커밋(양쪽 레포) → **계획서 전체 점검** | | 대기 | |

## 5. 특이사항

- **두 레포에 걸친 M.** 서버 레포의 변경은 문서와 (필요 시) API 형식 조정뿐. Unity 쪽 커밋 메시지도 `M7: …` 형식을 따른다.
- **클라이언트 경험자가 서버를 이해하는 M.** 지금까지의 서버 결정(UNIQUE로 멱등, revision으로 충돌)이 클라 코드에서 어떻게 보이는지 — "seq를 클라가 매긴다"가 그 접점.
- **학습 포인트**: 오프라인 우선 설계, 큐와 멱등 키의 관계, 원자적 파일 쓰기.

## 6. 주의사항

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| C1 | 서버에 seq가 건너뛰어 들어옴 | 큐 일부 손실 / seq 파일과 큐 파일 불일치 | 한 파일에 같이 쓰기 |
| C2 | 온라인 복귀 시 409 | 다른 기기 플레이 | M8 |
| C3 | `chosenAt`이 로컬 시각 | `DateTime.Now` 사용 | `UtcNow.ToString("o")` |
| C4 | 스냅샷이 너무 큼 | StageState 전부 포함 | 정지 프레임에 필요한 최소만 — Unity 쪽 결정, 서버는 크기만 확인 (`snapshot` JSON 컬럼 최대 1GB이지만 `max_allowed_packet` 기본 64MB) |
| C5 | 앱 종료 시 저장 누락 | `OnApplicationQuit`에 의존 | 커밋 직후 저장 (이벤트 시점) — 종료 훅에 기대지 않음 |

## 7. 완료 기준 체크리스트

- [ ] 비행기 모드 선택 5개 → 온라인 → `choice_history` 5행, seq 연속.
- [ ] 강제 종료 후 재실행 → 로컬에서 이어짐, 큐 유지.

## 8. 갱신할 문서

- M7-7: `M7-check.md`, 필요 시 DECISIONS·M3 계획서(형식 변경).
- M7-8: STATUS.md, **M8·M9 점검**.

## 9. 다음 M에 넘기는 것

- M8: 409 시 큐 처리(폐기 vs force), 서버 슬롯 목록으로 복구.
